'use client';

import React from 'react';
import { StackingCards } from '@/components/ui/stacking-card';

const FeaturedProjectsStacked: React.FC = () => {
    return <StackingCards showHeader={true} />;
};

export default FeaturedProjectsStacked;